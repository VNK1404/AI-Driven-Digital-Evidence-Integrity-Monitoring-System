# Place your trained model file here:
#   best_model_ffpp.pth   — (86.25% val accuracy)
#
# This file is excluded from git (too large).
# Train your own model using: python train.py (from project root)
# Or request the pre-trained weights from the repository owner.
